import { number } from 'prop-types';

const Footertypes = {
  basePlannedTime: number.isRequired,
  workedTime: number.isRequired,
  dayOffTime: number.isRequired,
  deviationTime: number.isRequired,
};

export default Footertypes;
